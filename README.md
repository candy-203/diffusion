# Usage

In this repository you will find different implementations of diffiusion based image denoising. On the main branch you will find only empty jupyter notebooks. For the executed versions please switch to the executed branch.

If you want to try it yourself just make a virtual enviorment using `python -m venv venv` and install the dependencies via:

```bash
source venv/bin/activate
pip install -r requirments.txt
```